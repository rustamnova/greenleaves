# Greenleaves project instructions

- Read `docs/memory/` before changes. Actual code and validation outrank notes.
- Work only within this repository unless explicitly authorized.
- Contest stop-code is **2026-09-29 23:59 Europe/Moscow**. After it, do not push to submitted branches, deploy the site, or edit submitted documents/presentations. Continue only in a separate local/private copy detached from submitted links.
- Current code is an engineering prototype; never present unverified buffers as legally validated rules, generated points as approved planting, or a demo as validation of the 20-street dataset.
- Preserve partner provenance in `docs/audit/`; `legacy/python-v1` is an unmodified audit reference, not the supported application.
- Never publish private intake, archive passwords, browser sessions, personal correspondence or real engineering CAD files by default. `.private/` and archives stay ignored.
- Browser and CLI share `site/studio/engine.js`. Run meaningful geometry regressions: `node --test tests/*.test.mjs`.
- Keep source geometry unchanged in exported DXF. New entities only use GREENLEAVES_* layers.
- Do not create new accounts, spend money, message partners, or submit the competition form without task-specific authorization.
- Manual Pages release only; check stop-code before publication.
